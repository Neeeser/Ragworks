"""Evals subsystem: benchmark retrieval evaluation with trace attribution.

Public API is re-exported here as the subsystem grows; consumers import other
names from the owning submodule (e.g. `app.evals.metrics.registry`).
"""

from __future__ import annotations
